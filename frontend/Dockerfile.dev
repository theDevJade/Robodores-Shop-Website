FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* pnpm-lock.yaml* yarn.lock* ./
RUN npm install --include=dev

COPY . .

ENV FRONTEND_PORT=5173
EXPOSE ${FRONTEND_PORT}

CMD sh -c "npx vite --host 0.0.0.0 --port ${FRONTEND_PORT}"
